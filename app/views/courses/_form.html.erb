<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
  <div class="field">
    <%= form.label :title, class: "" %>
    <%= form.text_field :title, id: :course_title, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :course_type, class: "" %>
    <%= form.text_field :course_type, id: :course_course_type, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :instructor_id, class: "" %>
    <%= form.number_field :instructor_id, id: :course_instructor_id , class: "form-control"%>
  </div>

  <div class="field">
    <%= form.label :topic_id, class: "" %>
    <%= form.number_field :topic_id, id: :course_topic_id, class: "form-control" %>
  </div>

</div>

  <div class="actions">
    <%= form.submit %>
  </div>



<% end %>
